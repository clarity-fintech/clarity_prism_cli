# Git attribution

Commits attribute to Will Ferguson. Cursor co-author trailers are stripped via `.githooks/prepare-commit-msg`.

Enable hooks locally:

```bash
git config core.hooksPath .githooks
```

See `.mailmap` for author mapping.
