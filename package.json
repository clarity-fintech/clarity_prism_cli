{
  "name": "clarity-prism-cli",
  "private": true,
  "version": "1.0.2",
  "description": "CLRTY PRISM + HELIX download ecosystem — intelligence, execution, skills SDK, and CLI",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "node scripts/build_all.mjs",
    "build:terminal": "npm run build -w prism-cli",
    "dev:terminal": "node scripts/dev-terminal.mjs",
    "clean": "npm run clean --workspaces --if-present",
    "clrt": "node apps/cli/dist/index.js",
    "verify": "node scripts/verify_packages.mjs && node scripts/generate_data_inventory.mjs && node scripts/verify_cross_repo.mjs",
    "postinstall": "node scripts/check_repo_root.mjs"
  },
  "bin": {
    "clrt": "./bin/clrt"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
